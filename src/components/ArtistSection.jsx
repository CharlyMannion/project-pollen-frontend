import React from "react";

const ArtistSection = ()=> {
    
  return (
    <section className="artist-section container">
        <h2>Our Artists</h2>
        <p>"People think it can never happen to them, but it can, in the blink of an eye"</p>
    
    </section>
  );
};

export default ArtistSection;